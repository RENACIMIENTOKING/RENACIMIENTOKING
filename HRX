local Players = game:GetService("Players")
local VirtualUser = game:GetService("VirtualUser")
local ReplicatedStorage = game:GetService("ReplicatedStorage")

local LocalPlayer = Players.LocalPlayer

----------------------------------------------------------------
-- 1. WHITELIST Y ACCESO
----------------------------------------------------------------
local allowedUsernames = {
    "lucas09love2",
    "ianjajajjajajapolo1",
    "XxrenacimientosxX",
}

local isAllowed = false
local playerUsername = string.lower(LocalPlayer.Name)

for _, username in ipairs(allowedUsernames) do
    if playerUsername == string.lower(username) then
        isAllowed = true
        break
    end
end

if not isAllowed then
    warn("Acceso denegado, este Script es de compra.")
    LocalPlayer:Kick("No tienes permiso para ejecutar este Script.")
    return
end

----------------------------------------------------------------
-- 2. ANTI-AFK
----------------------------------------------------------------
LocalPlayer.Idled:Connect(function()
    VirtualUser:Button2Down(Vector2.new(0, 0), workspace.CurrentCamera.CFrame)
    task.wait(1)
    VirtualUser:Button2Up(Vector2.new(0, 0), workspace.CurrentCamera.CFrame)
    print("Anti-AFK: Prevenido desconexión")
end)

----------------------------------------------------------------
-- 3. INTERFAZ GRÁFICA (GUI)
----------------------------------------------------------------
local library = loadstring(game:HttpGet("https://pastebin.com/raw/Wbv9FYv2", true))()

local window = library:AddWindow("Clan HRX Hub | Private", {
    main_color = Color3.fromRGB(0, 191, 255),
    min_size = Vector2.new(600, 500),
    can_resize = true,
})

local AutoFarm = window:AddTab("Fast Farm")
local Estadisticas = window:AddTab("Stats")

----------------------------------------------------------------
-- 4. MANEJO DE MASCOTAS (ESTILO SILENCE)
----------------------------------------------------------------
-- Fuerza: Swift Samurai y Energy Core Dog
local FarmPets = {"Swift Samurai", "Energy Core Dog", "Perro de núcleo de energía"}

-- Rebirth: Titanium Hydra y Tribal Overlord
local RebirthPets = {"Titanium Hydra", "Hidra de titanio", "Tribal Overlord"}

local function unequipAllPets()
    local petsFolder = LocalPlayer:FindFirstChild("petsFolder")
    if not petsFolder then return end
    for _, folder in pairs(petsFolder:GetChildren()) do
        if folder:IsA("Folder") then
            for _, pet in pairs(folder:GetChildren()) do
                ReplicatedStorage.rEvents.equipPetEvent:FireServer("unequipPet", pet)
            end
        end
    end
    task.wait(0.1)
end

local function equipPetList(petList)
    local petsFolder = LocalPlayer:FindFirstChild("petsFolder")
    if not petsFolder then return end

    for _, folder in pairs(petsFolder:GetChildren()) do
        if folder:IsA("Folder") then
            for _, pet in pairs(folder:GetChildren()) do
                for _, targetName in ipairs(petList) do
                    if string.lower(pet.Name) == string.lower(targetName) then
                        ReplicatedStorage.rEvents.equipPetEvent:FireServer("equipPet", pet)
                    end
                end
            end
        end
    end
end

----------------------------------------------------------------
-- 5. FAST REBIRTH / FAST FARM
----------------------------------------------------------------
local leaderstats = LocalPlayer:WaitForChild("leaderstats")
local rebirthsStat = leaderstats:WaitForChild("Rebirths")

local footerTimeLabel = AutoFarm:AddLabel("Tiempo: 0d 0h 0m 0s")
footerTimeLabel.TextSize = 18
local footerRebirthsLabel = AutoFarm:AddLabel("Rebirths: 0")
footerRebirthsLabel.TextSize = 18
local footerRebirthsGainedLabel = AutoFarm:AddLabel("Rebirths Ganados: 0")
footerRebirthsGainedLabel.TextSize = 18

local startTime = tick()
local initialRebirths = rebirthsStat.Value

task.spawn(function()
    while true do
        local elapsed = tick() - startTime
        local days = math.floor(elapsed / 86400)
        local hours = math.floor((elapsed % 86400) / 3600)
        local minutes = math.floor((elapsed % 3600) / 60)
        local seconds = math.floor(elapsed % 60)
        footerTimeLabel.Text = string.format("Tiempo: %dd %dh %dm %ds", days, hours, minutes, seconds)
        task.wait(1)
    end
end)

local function updateRebirths()
    local current = rebirthsStat.Value
    local gained = current - initialRebirths
    footerRebirthsLabel.Text = "Rebirths: " .. current
    footerRebirthsGainedLabel.Text = "Rebirths Ganados: " .. gained
end

rebirthsStat.Changed:Connect(updateRebirths)
updateRebirths()

getgenv().AutoFarming = false

local function getGoldenRebirthCount()
    local ultimates = LocalPlayer:FindFirstChild("ultimatesFolder")
    if ultimates and ultimates:FindFirstChild("Golden Rebirth") then
        return ultimates["Golden Rebirth"].Value
    end
    return 0
end

local function getStrengthRequiredForRebirth()
    local rebirths = LocalPlayer.leaderstats.Rebirths.Value
    local baseStrength = 10000 + (5000 * rebirths)
    local golden = getGoldenRebirthCount()
    if golden >= 1 and golden <= 5 then
        baseStrength = baseStrength * (1 - golden * 0.1)
    end
    return math.floor(baseStrength)
end

AutoFarm:AddSwitch("Fast Rebirth / Fast Farm", function(state)
    getgenv().AutoFarming = state

    if state then
        warn("⚡ Fast Farm ACTIVADO")
        task.spawn(function()
            while getgenv().AutoFarming do
                local requiredStrength = getStrengthRequiredForRebirth()

                -- Fase 1: Equipar mascotas de Fuerza (Swift Samurai / Energy Core Dog)
                unequipAllPets()
                equipPetList(FarmPets)

                while LocalPlayer.leaderstats.Strength.Value < requiredStrength and getgenv().AutoFarming do
                    for _ = 1, 10 do
                        if LocalPlayer:FindFirstChild("muscleEvent") then
                            LocalPlayer.muscleEvent:FireServer("rep")
                        end
                    end
                    task.wait()
                end

                if not getgenv().AutoFarming then break end

                -- Fase 2: Equipar mascotas de Rebirth (Titanium Hydra / Tribal Overlord)
                unequipAllPets()
                equipPetList(RebirthPets)

                local oldRebirths = LocalPlayer.leaderstats.Rebirths.Value
                repeat
                    ReplicatedStorage.rEvents.rebirthRemote:InvokeServer("rebirthRequest")
                    task.wait(0.1)
                until LocalPlayer.leaderstats.Rebirths.Value > oldRebirths or not getgenv().AutoFarming
            end
        end)
    else
        warn("🛑 Fast Farm DESACTIVADO")
    end
end)

----------------------------------------------------------------
-- 6. STATS TRACKER EN TIEMPO REAL
----------------------------------------------------------------
local SelectPlayerName = ""

local PlayerDrop = Estadisticas:AddDropdown("Select Player", function(Value)
    SelectPlayerName = Value:match("| (.+)") or Value
end)

local function refreshDropdown()
    for _, Plr in pairs(Players:GetPlayers()) do
        PlayerDrop:Add(Plr.DisplayName .. " | " .. Plr.Name)
    end
end

refreshDropdown()

local function FormatNumberWithCommas(number)
    local formatted = tostring(number):reverse():gsub("(%d%d%d)", "%1,"):reverse()
    return formatted:gsub("^,", "")
end

local function FormatAbbreviated(number)
    local abbreviations = {"", "K", "M", "B", "T", "Qa", "Qi"}
    local index = 1
    while number >= 1000 and index < #abbreviations do
        number = number / 1000
        index = index + 1
    end
    return string.format("%.2f", number) .. abbreviations[index]
end

local function FormatDisplay(value)
    return "[ " .. FormatNumberWithCommas(value) .. " | " .. FormatAbbreviated(value) .. " ]"
end

local Update1 = Estadisticas:AddLabel("Gems: -")
local Update2 = Estadisticas:AddLabel("Strength: -")
local Update3 = Estadisticas:AddLabel("Agility: -")
local Update4 = Estadisticas:AddLabel("Durability: -")
local Update5 = Estadisticas:AddLabel("Rebirths: -")
local Update6 = Estadisticas:AddLabel("Muscle King Time: -")
local Update10 = Estadisticas:AddLabel("Custom Size: -")
local Update11 = Estadisticas:AddLabel("Custom Speed: -")
local Update12 = Estadisticas:AddLabel("Evil Karma: -")
local Update13 = Estadisticas:AddLabel("Good Karma: -")

task.spawn(function()
    while task.wait(0.5) do
        if SelectPlayerName ~= "" then
            local targetPlayer = Players:FindFirstChild(SelectPlayerName)
            if targetPlayer then
                if targetPlayer:FindFirstChild("Gems") then
                    Update1.Text = "Gems: " .. FormatDisplay(targetPlayer.Gems.Value)
                end
                if targetPlayer:FindFirstChild("Agility") then
                    Update3.Text = "Agility: " .. FormatDisplay(targetPlayer.Agility.Value)
                end
                if targetPlayer:FindFirstChild("Durability") then
                    Update4.Text = "Durability: " .. FormatDisplay(targetPlayer.Durability.Value)
                end
                if targetPlayer:FindFirstChild("muscleKingTime") then
                    Update6.Text = "Muscle King Time: " .. FormatDisplay(targetPlayer.muscleKingTime.Value)
                end
                if targetPlayer:FindFirstChild("customSize") then
                    Update10.Text = "Custom Size: " .. FormatDisplay(targetPlayer.customSize.Value)
                end
                if targetPlayer:FindFirstChild("customSpeed") then
                    Update11.Text = "Custom Speed: " .. FormatDisplay(targetPlayer.customSpeed.Value)
                end
                if targetPlayer:FindFirstChild("evilKarma") then
                    Update12.Text = "Evil Karma: " .. FormatDisplay(targetPlayer.evilKarma.Value)
                end
                if targetPlayer:FindFirstChild("goodKarma") then
                    Update13.Text = "Good Karma: " .. FormatDisplay(targetPlayer.goodKarma.Value)
                end

                local targetLeaderstats = targetPlayer:FindFirstChild("leaderstats")
                if targetLeaderstats then
                    if targetLeaderstats:FindFirstChild("Strength") then
                        Update2.Text = "Strength: " .. FormatDisplay(targetLeaderstats.Strength.Value)
                    end
                    if targetLeaderstats:FindFirstChild("Rebirths") then
                        Update5.Text = "Rebirths: " .. FormatDisplay(targetLeaderstats.Rebirths.Value)
                    end
                end
            end
        end
    end
end)
